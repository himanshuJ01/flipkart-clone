server:
   port: 9092
   
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: flipkartOrder


  application:
    name: ORDER-SERVICE
 
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost  