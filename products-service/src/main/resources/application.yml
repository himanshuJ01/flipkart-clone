server:
   port: 9091
   
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: flipkartProduct
 
  application:
    name: PRODUCT-SERVICE

 
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

data:
  exception:
    msg: Product found already.....!!         